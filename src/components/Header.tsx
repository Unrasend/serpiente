import React from 'react';
import './Header.scss';

export const Header = () => {
  return (
    <header className="terminal-header">
      <a href="#" className="logo link after:hidden before:hidden">Serpiente</a>
    </header>
  );
};
